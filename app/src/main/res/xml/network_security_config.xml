<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Los proveedores IPTV frecuentemente sirven streams HTTP. El usuario
         añade explícitamente la fuente; no hay dominios precargados. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
